# Windows / WSL zone identifier metadata files
*:Zone.Identifier

# Process ID files
*.pid

# ── Deep Dive research temp / checkpoint files ───────────────────────────────
# Slither/Aderyn run checkpoints (large, regeneratable)
*checkpoint*.jsonl
# Root-level temp files from analysis runs
/report.json
/tmp_aderyn_fix.py
/test
# Phase 3 solc binary downloads (regeneratable)
/Phase3_DeepAnalysis_2026-06-06/solc-binaries/

# ── ML data (large / regeneratable) ─────────────────────────────────────────
# Raw datasets (GBs — not tracked; obtain from original source)
/docs/archive/
/BCCC-SCsVul-2024/
ml/data/SolidiFI/
ml/data/SolidiFI-benchmark/
ml/data/SolidiFI-processed/
ml/data/smartbugs-curated/
ml/data/smartbugs-results-master/
ml/data/smartbugs-wild/

# Generated graph / token datasets (rebuild via graph_extractor + tokenizer)
ml/data/graphs/
ml/data/graphs_legacy/
ml/data/tokens_windowed/
ml/data/tokens_orphaned/
ml/data/tokens_test/
ml/data/archive/

# Cached datasets (rebuild with create_cache.py)
ml/data/cached_dataset*.pkl

# Processed CSV / parquet index files (generated by label_cleaner + preprocessing)
ml/data/processed/

# Runtime / temp data
ml/data/reports/
ml/data/test_chunk_results.json
ml/data/test_fixed_export.json
ml/data/validation_report.json
.kiro/
.mimocode/
.kilo/

# ── MLflow ──────────────────────────────────────────────────────────────────
mlruns.db
mlruns/

# ── Assistant/local workspace state ─────────────────────────────────────────
.claude/

# ── ZKML large binaries ──────────────────────────────────────────────────────
zkml/ezkl/proving_key.pk
zkml/ezkl/srs.params

# ── Agent runtime data ───────────────────────────────────────────────────────
agents/data/

# ── Training / process output ────────────────────────────────────────────────
ml/logs/
logs/
nohup.out
slither_output.json
validation_full_output.txt

# ── Local tooling ────────────────────────────────────────────────────────────
.cache/
.local-bin/

# Documentation backup and temporary files
*.tmp
*.backup
*.bak

# Log files
*.log

# Environment files / secrets
.env
.env.local
.env.*
# Allow safe committed templates for fresh-clone setup.
!.env.example
!**/.env.example

# Slither/crytic build-cache artifacts (regenerated on every run)
crytic-export/
*.call-graph.dot

# Editor/IDE files
.vscode/
.idea/
*.swp
*.swo

# Python-specific generated state
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
coverage/
htmlcov/

# Dependencies / virtual environments
node_modules/
venv/
.venv/

# Build artifacts
dist/
build/
target/
.gradle/

# System files
.DS_Store
Thumbs.db

# Compressed files
*.zip
!docs/plan/ml-R4/review_bundles/r4_gap007_candidate2_independent_review_v1.zip
*.gz
*.tar
*.tgz
*.bz2
*.xz
*.7z
*.rar
*.zst
*.lz4
*.lzh
*.cab
*.arj
*.rpm
*.deb
*.Z
*.lz
*.lzo
*.tar.gz
*.tar.bz2
*.tar.xz
*.tar.zst

docs/IRVLESS*.txt
IRVLESS*.txt

# ── SmartBugs Wild eval large files (regeneratable from state) ───────────────
ml/data/smartbugs_wild_eval_state.json

# ── SmartBugs Wild eval large per-contract/contamination outputs ─────────────
docs/reports/**/per_contract*.json
docs/reports/**/contamination_index.json

# DVC cache/runtime/local configuration state
.dvc/cache/
.dvc/tmp/
.dvc/config.local

# Root-level runtime helpers generated during local troubleshooting
/tmp_*.py
/check_verify.sh
/submit_audit.sh
