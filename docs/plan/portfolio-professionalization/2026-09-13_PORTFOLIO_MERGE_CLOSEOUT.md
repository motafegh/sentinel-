# Sentinel Portfolio Professionalization — Merge Closeout

**Date:** 2026-09-13  
**PR:** #72  
**Merge commit:** `6e86b04e9d78ab9d0e6fd4a485bec7270bec0b7c`  
**Disposition:** `MERGED_TO_MAIN / RELEASE_SECURITY_FOLLOWUP_OPEN`

## Result

The P0–P8 Sentinel portfolio-professionalization program was merged into `main` through PR #72 after the exact PR head `e2022b23002a01f7562c20895065462b94b9c01a` completed the current pull-request CI surfaces successfully.

The merge preserves the full branch history rather than squashing away the evidence trail.

## What is now on `main`

- recruiter/senior-engineer-oriented root README;
- explicit contribution-era / AI-assistance boundary;
- canonical architecture/trust presentation;
- current R4 authority validator and public validation guide;
- dependency-light fresh-clone showcase;
- DATA lock/reproducibility CI;
- repository secret-scanning controls and historical baseline policy;
- MIT root license plus `THIRD_PARTY_NOTICES.md` scope boundary;
- seven evidence-first engineering case studies;
- P0–P8 portfolio planning, decision, validation, and closeout records.

## Technical truth preserved

The merge does not change current DATA/ML research authority:

- historical G0–G7 remain PASSED and immutable;
- Phase 8 / G8 remains open;
- R4-D-011 remains the exact accepted V10 V2.6 physical representation authority;
- R4-D-012 still requires a fresh guarded-selector successor lineage and separate physical acceptance;
- confirmed negatives remain zero;
- Run12 remains the historical operational ML baseline;
- no repaired R4 teacher has been trained/promoted;
- threshold/calibration/untouched-acceptance support remains unavailable;
- full repaired training remains unauthorized;
- the live audit MCP remains read-only;
- no production signer/broadcaster is claimed;
- retained ZK assurance remains proxy-only and `check_mode="UNSAFE"` remains an explicit limitation.

## Merge validation

Before merge, the final PR head had successful current workflow runs for:

- Handbook;
- Portfolio showcase;
- DATA reproducibility;
- SENTINEL system alignment;
- Security hygiene.

After this administrative closeout is committed to `main`, the current `main` workflow state should be re-observed and recorded by the canonical status ledger.

## Remaining non-blocking repository polish

The current GitHub connector cannot delete refs or mutate repository description/topics. Therefore these remain operational polish items rather than reasons to roll back the merge:

- delete accidental no-work refs `tmp-should-not-create`, `noop`, and `ignore-me` when a ref-deletion-capable surface is available;
- optionally set the prepared repository description/topics when repository-settings mutation is available.

## Release security follow-up

A historical provider-RPC credential-shaped endpoint remains recorded in the reviewed Git history baseline. Repository controls are clean, but Git cannot prove provider-side revocation/rotation.

Do not probe or reuse the historical value. Before publishing a formal portfolio release/tag, revoke/rotate it if it represented a live credential or independently confirm that revocation/rotation already occurred.

The repository merge itself is complete; this external credential disposition remains a release-specific security follow-up.
