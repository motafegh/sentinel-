# Independent Audit — Agent 1's DIVE Data-Source Quality Investigation

**Date:** 2026-06-19
**Auditor:** Independent Claude agent (read-only / audit-only; did not communicate with or influence Agent 1)
**Subject:** Agent 1's work under `docs/plan/data_module/2026-06-18-dive-data-source-quality-investigation/`
**Governing protocol audited against:** `docs/plan/data_module/CLAUDE.md` (the data_module protocol) + root `CLAUDE.md`
**Scope of this audit:** All finding files Agent 1 produced (`01_dive_methodology.md` = Method 1, `02_folder_csv_agreement.md` = Method 2, `08_parser_faithfulness.md` = Method 8) plus the in-progress Method 0 (`METHOD_0_HANDOFF.md`; no `00_tp_criteria_v1.md` exists yet). The task brief framed this as "the ONE Method," but Agent 1 in fact ran four Methods (8 → 2 → 1 → 0-in-progress); this audit covers the whole body of produced work and is named for the three completed findings.
**Audit method:** Independently re-derived every headline number with a from-scratch script (`/tmp/opencode/audit_independent_verify.py`, written without importing or copying Agent 1's scripts), spot-checked Agent 1's self-verify examples, verified cited artifacts exist on disk, and verified the M1 paper DOI resolves.

---

## 1. Process compliance (per `docs/plan/data_module/CLAUDE.md`)

| # | Checklist item (task + protocol) | Verdict | Evidence |
|---|---|---|---|
| A1 | EXPLAIN before EXECUTE + wait for explicit go (§1) | **INCONCLUSIVE** | The §11 execution log marks every row "Explained ✓" and "Ali confirmed start ✓ (Ali asked)". I have no access to the conversation, only Agent 1's own log, so I **cannot independently verify** the confirmations — exactly the situation the task warned about ("check the conversation/log, not just its own claim"). The notation "(Ali asked)" on every row suggests **Ali initiated** each Method rather than Agent 1 explaining-then-Ali-confirming, which **inverts the §1 direction** (AI-EXPLAIN → Ali-CONFIRM). Whether Agent 1 also explained before executing in those cases is not recoverable from the artifacts. Flagged, not condemned. |
| A2 | Findings written PROGRESSIVELY during execution (§10, amended) | **FAIL** | `git log --follow` on each of `01/02/08_*.md` returns exactly **one** commit: `fec036a87` (Jun 19 00:55, authored by Ali himself). `git show --stat fec036a87` shows ALL three findings + BOTH scripts + the protocol `CLAUDE.md` + the README + both tool references + the handoff = **10 files, 2447 insertions, one commit**. The commit message itself states: *"BACKFILL: this is a one-time catch-up commit … **Not incremental evidence.** From this point forward, each sub-step within a Method will be committed individually."* No in-file timestamped append-log exists in any finding file (all three have a single "Started/Completed" header, no per-sub-step dated lines). This directly violates §10's "written progressively DURING execution … not reconstructed at the end" and "Progressive documentation must be CHECKABLE … commits OR in-file timestamped append-log." Agent 1 acknowledges the violation and commits to incremental commits going forward. |
| A3 | Stay within the ONE Method authorized / no drift | **PASS (with caveat)** | Agent 1 ran Methods 8, 2, 1, 0. The task's "ONE Method" framing appears to be a generic template; per the README dependency map, M2 and M8 are explicitly **independent** ("can run any time") and M1/M0 are **prerequisites**, so running all four is within the plan's authorized ordering. **No drift** into M3–M6 (which are gated on M0 criteria freeze). Method 0's Batch 1 TP/FP/BORDERLINE judgements are within Method 0's authorized scope (worked-example pre-judgement is part of criteria development). Caveat: Method 0's deliverable `00_tp_criteria_v1.md` is not yet produced (see A-provenance + §3 below). |
| A4 | Provenance for every number: command, seed, tool+version, input corpus count/hash + frame, output path, timestamp (§10) | **PARTIAL FAIL** | M8 and M2 record command, RNG seed (N/A — full corpus), tool (Python 3.12), input paths **with frames** (raw vs v3-export), script path, and a date timestamp. **MISSING in all findings: input corpus *hash*** (§10 requires "a hash **or** count" — they record count but not hash; for reference I computed `DIVE_Labels.csv` sha256 = `a260946ec7741ca4212648a0568f419a32e7cac96c9c0dca1fe3d812bb43f029`, which none of the findings cite). **Timestamps are date-only** ("2026-06-18"), no time, so runs cannot be ordered within the day. **Method 0 Batch 1**: the seed (`20260618`) is named in handoff prose but there is **no `samples/` directory** (README §7 lists it as a deliverable) and **no exact selection command** recorded — the sample is not reproducible from the artifacts alone. M8's hardcoded crosswalk copy IS explicitly flagged in its provenance header ("script hardcodes copy"), which satisfies §7's "flag the copy explicitly" option; I verified the hardcoded copy currently equals the live `dive.yaml`. |
| A5 | State explicitly what it did NOT cover/verify | **PASS** | All three findings have explicit "What was NOT covered / NOT checked" sections (M8 lines 68–72, 147–151, 217–222; M2 lines 90–93; M1 lines 100–104). M8's self-critique (lines 226–242) additionally documents what went wrong in its first pass. |
| A6 | No extrapolation of batch/sample results to a larger population without explicit authorization (§3) | **PASS** | M8, M2, M1 are **full-corpus** structural checks (no sampling, no rates projected to a population). Method 0 Batch 1 reports **batch-only** stats ("EB: 5/5 FP. RE: 0 TP, 3 BORDERLINE, 2 FP, 2 correctly zero") with no projected corpus totals. The one qualitative generalization — "Meme tokens (20724, 21559, 1607) share a Uniswap-swap-before-state CEI" (handoff line 47) — is a pattern observation from 3 contracts, presented as a pattern, not as a rate extrapolated to the corpus. Borderline-acceptable; no CI attached but no population inference drawn. |
| A7 | Cite the actual command/file/data behind every claim (§10) | **PASS (with minor gaps)** | M8 cites: `patch_dos_v3.py` path, `2026-06-13_project_dos_patch.md` memory path, test name `test_dive_dos_reentrancy_cooccurrence_finding`, the `tier:null` signature, and the 2,655 count. M2 cites CSV path, folder paths, script. M1 cites paper DOI + Zenodo DOIs. **Minor gaps**: (a) M1 attributes internal stats to "the authors report" — "14.3% false positives in DoS and 24.9% … in Time Manipulation" and two direct quotes — without page/section/figure citations, so a reader cannot locate them in the paper; (b) M8 line 149 makes an uncited factual sub-claim that is actually **wrong** (see §3-D6). |

### Additional process observations (not in the task checklist but required by the protocol)

- **§1/§2 WHAT-vs-WHY and "history is a required lead": PAST VIOLATION, corrected.** Method 8's *first pass* diagnosed the 2,655 DoS mismatches as a "parser cache bug / stale-cache / incomplete run" — a **fabricated plausible mechanism** (the exact failure mode §1 warns against: "a coherent, internally-consistent, confidently-worded explanation is *exactly* what a wrong root cause looks like"). It was corrected **only after Ali pointed to MEMORY.md + the patch script** (per §11 execution-log row 8: "corrected after Ali pointed to MEMORY.md + patch script"). The corrected finding (current `08_parser_faithfulness.md`) now properly evidences the root cause and includes a self-critique section. Notably, the `CLAUDE.md` protocol was **hardened after this failure** (README §11: "causal-diagnosis hardening — triggered by the Method 8 DoS misdiagnosis"), so the protocol in force today is stronger than the one under which the mistake was made. The corrected M8 is compliant; the fact that the mistake happened at all is a documented process failure that was caught.
- **§4 Criteria frozen before counting (Method 0): NOT YET SATISFIED.** The required deliverable `findings/00_tp_criteria_v1.md` does **not exist** on disk. The execution log and handoff claim Batch 1 (10 contracts) is complete and "Practical floor ≥10% TP confirmed (2026-06-19)", but the frozen/signed criteria file is absent, so no TP-measurement Method (M3–M6) is currently authorized to start. Agent 1 correctly marks Method 0 "IN PROGRESS" (not complete), so this is not a false-claim-of-completion; it is an unfinished prerequisite. The "≥10% floor confirmed" claim is not verifiable from artifacts (no signed criteria file, no conversation access).

---

## 2. Factual correctness — independently re-derived numbers vs Agent 1's claimed numbers

I wrote `/tmp/opencode/audit_independent_verify.py` from scratch (stdlib only; does not import or copy Agent 1's `parser_faithfulness.py` or `verify_folder_csv_agreement.py`). I independently re-derived the crosswalk by reading `data_module/sentinel_data/labeling/crosswalks/dive.yaml` myself (confirmed identical to Agent 1's hardcoded copy). All counts below are from that independent run.

### 2.1 Structural counts (frame: raw = 22,330; v3-export = 22,073)

| Quantity | Agent 1 claimed | Independently re-derived | Match |
|---|---:|---:|:---:|
| CSV data rows (raw) | 22,330 | 22,330 (`csv.DictReader`; `wc -l`=22,330 consistent with README's no-trailing-newline note) | ✓ |
| `__source__` .sol files | 22,330 | 22,330 | ✓ |
| `meta.json` files (v3-export) | 22,073 | 22,073 | ✓ |
| `labels.json` files (v3-export) | 22,073 | 22,073 | ✓ |
| Raw → export drop | 257 | 257 | ✓ |
| cid→sha mapping 1:1, 0 collisions, 0 parse failures | yes | 22,073 mapped, 0 cid-collisions, 0 sha-collisions, 0 parse-fail | ✓ |
| labels.json exist for every mapped sha | 22,073/22,073 | 22,073/22,073 | ✓ |

### 2.2 Per-class CSV `=1` counts (raw frame) — README §2.2 / M2 / M8

| Class (CSV column) | Agent 1 | Re-derived | Match |
|---|---:|---:|:---:|
| Access Control | 16,723 | 16,723 | ✓ |
| Reentrancy | 11,400 | 11,400 | ✓ |
| Arithmetic | 9,542 | 9,542 | ✓ |
| Time manipulation | 6,322 | 6,322 | ✓ |
| Unchecked Return Values | 5,911 | 5,911 | ✓ |
| DoS | 3,781 | 3,781 | ✓ |
| Bad Randomness | 634 | 634 | ✓ |
| Front Running | 606 | 606 | ✓ |

### 2.3 Per-folder symlink counts (raw frame) — README §2.1 / M2

| Folder | Agent 1 | Re-derived | Match |
|---|---:|---:|:---:|
| Access Control | 16,723 | 16,723 | ✓ |
| Reentrancy | 11,400 | 11,400 | ✓ |
| Arithmetic | 9,542 | 9,542 | ✓ |
| Time manipulation | 6,322 | 6,322 | ✓ |
| Unchecked Return Values | 5,911 | 5,911 | ✓ |
| DoS | 3,781 | 3,781 | ✓ |
| Bad Randomness | 634 | 634 | ✓ |
| Front Running | 606 | 606 | ✓ |
| **TOTAL symlinks** | 54,919 | 54,919 | ✓ |

### 2.4 Method 2 per-contract identity (M2 headline: 0 / 178,640 mismatches)

| Quantity | Agent 1 | Re-derived | Match |
|---|---:|---:|:---:|
| Total cids compared | 22,330 | 22,330 | ✓ |
| Total (cid × class) pairs | 178,640 | 178,640 (= 22,330 × 8) | ✓ |
| Total mismatches | 0 | 0 | ✓ |
| Per-class csv1/folder0 | 0 (all 8) | 0 (all 8) | ✓ |
| Per-class csv0/folder1 | 0 (all 8) | 0 (all 8) | ✓ |

### 2.5 Method 8 CSV-vs-parsed per-class agreement (M8 headline)

| Class | Agent 1 (match/total) | Re-derived (match/total) | Mismatches (re-derived) | Match |
|---|---|---|---:|:---:|
| ExternalBug | 22,073 / 22,073 | 22,073 / 22,073 | 0 | ✓ |
| IntegerUO | 22,073 / 22,073 | 22,073 / 22,073 | 0 | ✓ |
| Reentrancy | 22,073 / 22,073 | 22,073 / 22,073 | 0 | ✓ |
| Timestamp | 22,073 / 22,073 | 22,073 / 22,073 | 0 | ✓ |
| TransactionOrderDependence | 22,073 / 22,073 | 22,073 / 22,073 | 0 | ✓ |
| UnusedReturn | 22,073 / 22,073 | 22,073 / 22,073 | 0 | ✓ |
| DenialOfService | 19,418 / 22,073 (87.97%) | 19,418 / 22,073 | 2,655 | ✓ |
| DoS mismatch direction | "all 2,655 are csv=1/lab=0; zero csv=0/lab=1" | csv1/lab0 = 2,655; csv0/lab1 = 0 | — | ✓ |

### 2.6 DoS-patch signature (M8 root-cause evidence — independently re-derived)

| Signature check | Agent 1 claims | Re-derived | Match |
|---|---|---|:---:|
| All 2,655 mismatched have `tier=null` in labels.json | yes (2,655/2,655) | all 2,655 have `tier: None` | ✓ |
| All 2,655 have CSV Reentrancy=1 | yes (2,655/2,655) | all 2,655 have CSV Re=1 | ✓ |
| Count matches patch script's logged output | yes (2,655) | patch script `patch_dos_v3.py` line 78 prints "3,750 − 2,655 = 1,095" | ✓ |
| Patch script zeroes `value`, sets `tier=None`, keeps `source="dive"` | yes | confirmed at `patch_dos_v3.py` lines 53–57 | ✓ |
| Protecting test exists and asserts the patch | `test_dive_dos_reentrancy_cooccurrence_finding` asserts `len(flagged_dos) == 0` | test at `test_class_auditor.py:123`, assertion at line 145, comment at line 138 confirms revert-breakage | ✓ |

### 2.7 Method 8 drop breakdown (M8 Step 4)

| Class | Agent 1 dropped (of CSV-pos) | Re-derived | Match |
|---|---:|---:|:---:|
| Access Control (EB) | 141 (of 16,723) | 141 | ✓ |
| Arithmetic (IUO) | 154 (of 9,542) | 154 | ✓ |
| Reentrancy | 70 (of 11,400) | 70 | ✓ |
| Unchecked Return (UR) | 52 (of 5,911) | 52 | ✓ |
| Time manipulation (TS) | 50 (of 6,322) | 50 | ✓ |
| DoS | 31 (of 3,781) | 31 | ✓ |
| Bad Randomness | 2 (of 634) | 2 | ✓ |
| Front Running (TOD) | 2 (of 606) | 2 | ✓ |
| Zero-label dropped | 28 | 28 | ✓ |

### 2.8 Spot-checks of Agent 1's self-verify examples (re-done by me, not trusted)

**M8 self-verify table (cid 1, 2, 12727) — re-derived:**
| cid | Agent 1: CSV → expected → actual | Re-derived: CSV positives → expected canon → actual labels | Match |
|---|---|---|:---:|
| 1 | DoS=1 → {DoS} → {DoS} | {DoS} → {DenialOfService} → {DenialOfService} | ✓ |
| 2 | RE=1,DoS=1 → {RE,DoS} → {RE} (DoS missing) | {DoS,Reentrancy} → {Reentrancy,DenialOfService} → {Reentrancy} | ✓ |
| 12727 | RE=1,AC=1 → {RE,EB} → {RE,EB} | {Access Control,Reentrancy} → {Reentrancy,ExternalBug} → {Reentrancy,ExternalBug} | ✓ |

**M2 self-verify table (cid 1, 100, 1000) — re-derived:**
| cid | Agent 1: CSV → folders | Re-derived: CSV positives → folder set | Match |
|---|---|---|:---:|
| 1 | {DoS} → {DoS} | {DoS} → {DoS} | ✓ |
| 100 | {AC,Arith,RE,TS,UR} → same | {Reentrancy,Time manipulation,Arithmetic,Unchecked Return Values,Access Control} → same | ✓ |
| 1000 | {AC,Arith,RE} → same | {Arithmetic,Access Control,Reentrancy} → same | ✓ |

**Method 0 Batch 1 — 10 contract labels (re-derived from CSV):** all 10 cids (21797, 19142, 6235, 20724, 21559, 19601, 1607, 17235, 17404, 19323) have CSV labels **exactly** as the handoff attributes them (incl. 17404 and 19323 as zero-label). The TP/FP/BORDERLINE verdicts themselves are Agent 1's manual judgement — I did not re-judge them (out of audit scope, and the criteria file is not yet frozen/signed; see §3-D5).

### 2.9 Method 1 citation verification

| Claim | Verification | Result |
|---|---|:---:|
| DOI `10.1038/s41597-026-07025-5` resolves | `webfetch https://doi.org/10.1038/s41597-026-07025-5` | ✓ resolves to "Alsunaidi, S. J., Aljamaan, H., & Hammoudeh, M. (2026). DIVE: A Multi-Label Smart Contract Vulnerability Dataset. *Scientific Data*, 13(1)." |
| Authors / title / journal / year | M1: "Alsunaidi, Aljamaan, Hammoudeh", "DIVE: A Multi-Label…", *Scientific Data* 13, 2026 | ✓ all match the DOI redirect |
| "No local README/methodology files found" | `find` on `raw_staging/dive/` and `dive_labels/` | ✓ only the 8 class folders + `__source__` + the CSV; no `.md`/`.txt`/readme |
| Ingestion manifest `url: ""` | `json.load(ingestion_manifest.json)["url"]` | ✓ `''` |

**Note:** M1 corrects a stale prior-claim — the integration doc / README §6 originally said "Nature Sci. Data **2025**"; M1 says **2026**, and the DOI confirms 2026. M1 does **not** explicitly flag this as a §2 contradiction-finding (it silently embeds the correction). Minor.

### 2.10 Factual correctness summary
**Every headline number Agent 1 published reproduces exactly under independent re-derivation.** All 8 per-class CSV counts, all 8 folder counts, the 22,330/22,073/257 frames, the 178,640/0 M2 result, the 6-class-perfect + DoS-2,655 M8 result, the DoS-patch signature (tier=None ∧ RE=1 on all 2,655), the drop breakdown, and all 6 self-verify spot-checks match. The one factual error found is a secondary sub-claim in a caveat section (§3-D6), not a headline number.

---

## 3. Discrepancies, ghost assumptions, missing controls, and protocol violations (named explicitly)

- **D1 — §10 violation: Non-incremental documentation (HIGH).** All findings + scripts + protocol + plan committed in a single backfill commit `fec036a87`. The commit message itself admits "Not incremental evidence." No per-sub-step commits, no in-file timestamped append-log. This is the exact failure mode §10 was written to prevent ("A finding file written in one shot at the very end, even if accurate, does not satisfy this requirement"). Agent 1 acknowledges and promises incremental commits going forward — but the trail for the completed M1/M2/M8 is **not** inspectable after the fact, which is the specific harm §10 guards against.

- **D2 — §1/§2 violation (past, corrected): Fabricated root cause in M8's first pass (HIGH, now remediated).** The original M8 diagnosed the 2,655 DoS mismatches as a "parser cache bug" — a plausible but invented mechanism, constructed without first checking `MEMORY.md` / `git log` / the archived patch script, exactly the trap §1/§2 describe ("plausibility is not evidence"). Corrected only after Ali intervened. The current M8 finding is properly evidenced and includes a self-critique. The protocol was hardened after this failure (README §11). **Net: a real violation that was caught and fixed, not a live defect — but it is the single most serious process failure in the record and shows the protocol can be violated even while in force.**

- **D3 — §10 violation: Provenance gaps (MED).** (a) No input corpus **hash** recorded in any finding (only counts) — I computed `DIVE_Labels.csv` sha256 = `a260946ec7741ca4212648a0568f419a32e7cac96c9c0dca1fe3d812bb43f029`, not cited by Agent 1. (b) Timestamps are date-only, no time — runs within the same day cannot be ordered. (c) Method 0 Batch 1: seed `20260618` named in handoff prose, but **no `samples/` directory** and **no exact selection command** — the 10-contract sample is not reproducible from artifacts alone.

- **D4 — Unverifiable confirmation claims (MED, inherent limitation).** The §11 execution log's "Ali confirmed start ✓ (Ali asked)" entries cannot be verified from artifacts; I have no conversation access. The "(Ali asked)" notation further suggests Ali-initiated requests, which inverts the §1 AI-EXPLAIN→Ali-CONFIRM direction. I explicitly do **not** guess whether the confirmations happened — I report I cannot verify them.

- **D5 — §4 not yet satisfied: Method 0 criteria file missing (MED).** `findings/00_tp_criteria_v1.md` does not exist, so the TP/FP/BORDERLINE bar is **not frozen/signed**. Method 0 is correctly marked IN PROGRESS, and its Batch-1 judgements are within Method 0's authorized worked-example scope — but until `00_tp_criteria_v1.md` is signed, no M3–M6 TP measurement is authorized (README §4 gate). The execution-log claim "Practical floor ≥10% TP confirmed (2026-06-19)" is not verifiable from artifacts.

- **D6 — Factual error in M8 line 149 (LOW–MED, but a real factual defect).** M8 "What Step 3 did NOT cover" states: *"the other 18,323 DoS=0 labels are all correct CSV-DoS=0 contracts whose Reentrancy is also 0."* The count 18,323 (CSV-DoS=0 contracts in the compared set) is **correct**. But the clause "whose Reentrancy is also 0" is **FALSE**: only **9,648** of those 18,323 are also RE=0; the other **8,675** are RE=1. Verified independently. This is an uncritical assertion attached to a correct count — precisely the "plausible statement pinned on a real number" pattern §1 warns against. It does not affect M8's headline verdict (parser faithful; DoS mismatch = intentional patch), but it is a factual blemish a careful reader would catch and a downstream consumer might propagate.

- **D7 — Minor M1 attribution gaps (LOW).** M1's paper-internal claims — "6 tools", "Power-based voting", "14.3% FP in DoS, 24.9% in Time Manipulation", and the two direct quotes — are attributed to "the authors" without page/section/figure citations. I verified the paper exists and its citation metadata matches, but I did **not** verify the in-paper methodology claims (would require reading the full paper). Also: M1 does not explicitly flag the 2025→2026 year correction as a §2 contradiction-finding against the prior integration doc.

- **D8 — Ghost assumption check: none found in headline numbers.** I specifically looked for silent hardcoded copies (M8's crosswalk — flagged explicitly per §7, and verified equal to live `dive.yaml`), frame confusions (all findings correctly label raw vs v3-export frames), and extrapolation (none). No ghost assumptions detected in the structural claims.

---

## 4. Overall verdict

### **TRUSTWORTHY WITH CAVEATS**

**Reasoning:**

The **factual core is solid.** I independently re-derived every headline number Agent 1 published — all 8 per-class CSV counts, all 8 folder symlink counts, the 22,330/22,073/257 raw→export frame, the 178,640-pair / 0-mismatch Method 2 result, the 6-class-perfect + 2,655-DoS-mismatch Method 8 result, the DoS-patch signature (tier=None ∧ RE=1 on all 2,655), the full drop breakdown, and all six self-verify spot-checks (M8 cids 1/2/12727, M2 cids 1/100/1000) — and **every one matches exactly**. The cited artifacts (patch script, protecting test, MEMORY.md patch file, paper DOI) all exist and say what M8/M1 claim they say. On the numbers, Agent 1's structural findings (M1 methodology, M2 folder↔CSV identity, M8 parser faithfulness) are reproducible and accurate. The single factual error (D6, M8 line 149's "Reentrancy is also 0" clause) is in a caveat section and does not touch any headline result.

The **process has real, documented lapses** that prevent an unqualified TRUSTWORTHY verdict:
1. The most serious — M8's original fabricated "parser cache bug" root cause (D2) — was a textbook §1/§2 violation (plausibility over evidence, history not checked first). It was **caught and corrected** with a documented self-critique, and the protocol was hardened as a result, but it demonstrates the failure mode is live even under the protocol.
2. The documentation is **not progressively checkable** (D1): a single backfill commit, admitted by Agent 1. The §10 trail for the completed Methods cannot be inspected after the fact.
3. **Provenance is incomplete** (D3): no corpus hashes, date-only timestamps, no `samples/` file for Method 0.
4. **The Method 0 criteria freeze is not done** (D5): `00_tp_criteria_v1.md` does not exist, so the TP-judgement bar is not yet frozen/signed and the gating prerequisite for M3–M6 is unmet.
5. **Confirmation claims are unverifiable** from artifacts (D4).

None of the caveats invalidate the three completed structural findings (M1, M2, M8), whose numbers I reproduced independently. They do mean: (a) the going-forward process must actually commit incrementally and record full provenance (hashes + times + sample files), (b) Method 0 must produce the signed `00_tp_criteria_v1.md` before any TP-measurement Method starts, (c) the M8 line 149 factual error should be corrected, and (d) future root-cause claims must be checked against history **before** publication, not after Ali intervenes. The corrected M8 shows Agent 1 can self-correct; the fact that the correction was Ali-triggered rather than self-caught is the reason this is "with caveats" rather than unqualified.

**Bottom line for Ali:** Trust the M1/M2/M8 **numbers** — they reproduce. Do **not** trust the M8 *caveat* clause on line 149 (mathematically wrong). Treat the process as on-improvement: the structural findings are usable, but require the going-forward fixes (incremental commits, full provenance, frozen criteria file) before the next Methods run, and the M8 line-149 error should be fixed in-place.

---

## 5. What this audit did NOT do (explicit)

- Did **not** re-judge Method 0's Batch-1 TP/FP/BORDERLINE verdicts — that requires the frozen criteria (not yet signed) and full manual `.sol` review, which is out of audit scope. I verified only that the **input labels** Agent 1 attributed to those 10 contracts are correct.
- Did **not** verify the §11 execution-log "Ali confirmed" entries — no conversation access. Reported as unverifiable, not as false.
- Did **not** read the full DIVE paper — only verified the DOI resolves and the citation metadata matches. The paper-internal methodology claims (6 tools, voting, 14.3%/24.9% FP) are attributed-but-unlocated by Agent 1 and unverified-by-me.
- Did **not** modify, fix, or edit any of Agent 1's files (read-only audit, per the rules).
- Did **not** start any investigation Method myself (per the rules; stopping after this audit file).
