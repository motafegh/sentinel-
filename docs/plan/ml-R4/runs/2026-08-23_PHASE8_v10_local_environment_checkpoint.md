# Phase-8 V10 local environment checkpoint

Date: 2026-08-23
Status: PRIMARY ML RUNTIME CONFIRMED; LOCAL REPEAT EVIDENCE REQUIRED; PHYSICAL ACCEPTANCE REMAINS BLOCKED
Scope: R4-B008 structural-drift investigation only; no training or model-quality authority

## Reusable protected-local paths now resolved

The exact frozen Slither-0.10 V10 v2.3 structural reference is present locally at:

`data_module/data/representations-r4-v3-candidate-v2.3-structural-reference-6087dc6d`

Its `v10_candidate_binding_report.json` records:

- `binding_digest_sha256 = 6087dc6d76d781efbefe0c4984458d291790c38b1c55d852f48fd796222b0260`
- `extractor_version = v2.3-r4-call-semantics`
- `passed = true`
- `physical_acceptance = false`

The current protected v2.4 candidate remains:

`data_module/data/representations-r4-v3-candidate`

and reports extractor identity `v2.4-r4-call-semantics-compat` with diagnostic binding pass but no physical acceptance.

A retained pre-ICFG-fix v2.4 root also exists locally at:

`data_module/data/representations-r4-v3-candidate-v2.4-pre-icfg-fix-ba010d47`

It is historical evidence only and is not the structural reference for the current repeat probe.

## Local environment split confirmed

The repository working plan distinguishes the two relevant Slither environments:

- the locked DATA environment uses Slither 0.11.5;
- the ML environment / structurally stable V9/V10 baseline uses Slither 0.10.0 and is the required primary runtime for the 20-contract repeat investigation.

The local bounded check confirmed that `data_module/.venv/bin/python` is Python 3.12.3 and currently contains:

- `slither-analyzer = 0.11.5`
- `crytic-compile = 0.3.11`
- importable `slither` at `data_module/.venv/lib/python3.12/site-packages/slither/__init__.py`.

Therefore `data_module/.venv` must not be downgraded or repurposed for the primary 0.10.0 repeat generation.

## Primary ML runtime confirmed

The protected-local check on `ml/.venv/bin/python` confirmed the exact primary runtime required for the repeat investigation:

- interpreter: `/home/motafeq/projects/sentinel/ml/.venv/bin/python`
- Python: 3.12.3
- `slither-analyzer = 0.10.0`
- `crytic-compile = 0.3.11`
- Slither import: `ml/.venv/lib/python3.12/site-packages/slither/__init__.py`
- PyTorch: `2.5.1+cu124`
- `sentinel_data` imports successfully when `data_module` is on `PYTHONPATH`.

This is the correct interpreter for the 20-contract primary-runtime repeated generation. No environment mutation is required.

A local shell verification wrapper stopped before generation because the diagnostic snippet accidentally reused the variable name `slither` for the imported Slither module after storing the package-version string. The printed package metadata already proved exact `slither-analyzer = 0.10.0`; the stop was a wrapper-variable bug, not an environment or project failure. No candidate artifacts were generated by that stopped command.

## Primary repeat import-path requirement confirmed

The first bounded repeat-generation invocation then failed before writing the first contract because it used `PYTHONPATH=data_module` only. `sentinel_data.representation.r4_orchestrator._extract_one` imports `ml.src.data_extraction.windowed_tokenizer`, so the repository root must also be importable.

The required local invocation from `~/projects/sentinel` is therefore:

`PYTHONPATH=.:data_module ml/.venv/bin/python ...`

This is an invocation-path correction, not a source-code or runtime defect. It matches the established Phase-8 local command shape used elsewhere in this project. The failed repeat did not establish structural evidence and must not be counted as one of the required three generations.

## DATA launcher issue confirmed

Direct execution of:

`data_module/.venv/bin/slither --version`

fails because the generated console-script shebang points to the obsolete interpreter path:

`/home/motafeq/projects/sentinel/Data/.venv/bin/python`

while the current repository path is `data_module`. This launcher defect is real but secondary: the underlying DATA Python environment is healthy and intentionally carries Slither 0.11.5. It is not the primary runtime required by this structural-drift tranche.

The V10 generator validates the installed Slither version from Python package metadata, so repairing this console launcher is not required for the current bounded investigation.

## Next bounded action

Use `ml/.venv/bin/python` with `PYTHONPATH=.:data_module` to regenerate only the 20 unexpected structural-drift identities three times under exact Slither 0.10.0. Then compare the repeats, current v2.4 candidate, and frozen v2.3 reference with `p8_probe_v10_structural_drift.py`.

Do not restart the completed 26-contract parse-only repair. Do not launch training. Do not weaken the transition-audit acceptance rule before the repeat evidence is classified.
