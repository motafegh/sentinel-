# R4 Risk and Blocker Register

| ID | Type | Description | Impact | Mitigation | Status | Gate |
|---|---|---|---|---|---|---|
| R4-R001 | Risk | Agent repeats prior contract audits instead of reusing them. | High | Non-duplication policy and mandatory gap IDs. | MITIGATED | G1-G4 |
| R4-R002 | Risk | Historical zeros are treated as confirmed negatives. | Critical | Phase 2 reconstructed zero origins; Phase 3 materialized all 173,384 historical-zero rows as `UNKNOWN`; Phase 5 target-zero schema requires `CONFIRMED_NEGATIVE`; both historical v1 and repaired-v2 repository builders prohibit synthetic target zero. | OPEN | G2-G9 |
| R4-R003 | Risk | Tool-correlated evidence is counted as independent confirmation. | High | Independence groups explicit; Phase 4 blind semantic review and Phase 5 aggregation policy prohibit implicit vote counting. | MITIGATED | G3-G5 |
| R4-R004 | Risk | Architecture changes distract from label repair. | High | Architecture freeze retained; repaired repository work preserves graph schema v9 and `[4,512]`; long-input architecture changes require a separate decision. | MITIGATED | G8 |
| R4-R005 | Risk | Threshold/calibration reuse inflates evaluation. | Critical | Phase 6 freezes `THRESHOLD_FIT` and `CALIBRATION_FIT` as `UNSUPPORTED_EMPTY`; repaired-v2 builders preserve those empty roles. | OPEN | G6-G9 |
| R4-R006 | Risk | Web3Bugs declared enabled in historical config but entirely absent. | High | Phase 5 explicitly excludes Web3Bugs from the first vNext baseline. | MITIGATED | G5 |
| R4-R007 | Risk | GasException has zero trustworthy active support. | Medium | Phase 5 explicitly sets GasException to `SUPERVISION_DISABLED_PENDING_EVIDENCE`; repaired semantic builder also masks disabled classes. | MITIGATED | G5-G8 |
| R4-R008 | Risk | 836 historical v1 contracts had labels but no representations; audit showed much of the loss was pipeline-created rather than source-intrinsic. | Critical | Repaired-v2 rebuilt into new roots and physically validated 22,540/22,540 published contracts and 67,620/67,620 files with zero missing/invalid. Twenty-two remaining DIVE compile failures are explicit source attrition rather than silent representation loss. Historical v1 remains unchanged. | MITIGATED | G6-G8 |
| R4-R009 | Risk | locked_files.sha256 is stale. | Low | Stale lock is not a protected R4 artifact; current content hashes are the baseline. | OPEN | — |
| R4-R010 | Risk | Exact byte identity of the export artifact at Run12 launch is not proven from tracked evidence. | Medium | Preserve Phase-2 population reconciliation; protected historical labels hash and Phase-3 ledger are fixed lineage roots. | MITIGATED | G6-G8 |
| R4-R011 | Risk | BCCC v1.4 verified labels exist but are DEFERRED and materially disagree with raw BCCC. | High | Phase 5/6 keep BCCC deferred/not imported into the first baseline. | MITIGATED | G5-G6 |
| R4-R012 | Risk | DIVE EB TP count discrepancy in recovered historical review. | Low | Exact discrepancy preserved; Phase 5 masks DIVE EB regardless, so no current role depends on the disputed count. | MITIGATED | G4-G5 |
| R4-R013 | Risk | Historical quickstart benchmark contains invalid `NonVulnerable` mappings (including SmartBugs access_control and SolidiFI tx.origin), so SHA-clean OOD status does not imply trustworthy negative labels. | High | Phase 6 excludes quickstart from threshold/calibration/untouched-acceptance authority; use only as exposed/provenance/case-study material if needed. | MITIGATED | G6-G10 |
| R4-R014 | Risk | Historical nominal DATA label orchestration is incomplete: `sentinel-data label` remains a legacy no-op seam. | Medium | Phase 7 added a separate deterministic v1 build path; Phase-8 repair adds a separate repaired-v2 claims/ledger/publication pipeline. Historical `_run_label()` remains compatibility material and is not used to create current truth. | MITIGATED | G7-G8 |
| R4-R015 | Risk | No trustworthy, semantically valid, unexposed untouched-acceptance corpus exists in recovered repository evidence. | Critical | Both historical and repaired role schemas keep `UNTOUCHED_ACCEPTANCE = UNSUPPORTED_EMPTY_FROZEN`; a later promotion claim requires a new separately protected corpus. | OPEN | G6-G10 |
| R4-R016 | Risk | No trustworthy class-specific confirmed-negative support exists in policy v1, limiting model selection, threshold fitting, calibration, and final discrimination claims. | Critical | Repaired-v2 still has 899 effective loss cells and all target `1`; no pseudo-negative construction is allowed. Repository-side `confirmed_negative_evaluation.py`, policy/template files, deterministic pilot queue, and fail-closed dual-review validator are implemented. Actual class-specific confirmed-negative evidence has not yet been adjudicated, and accepted evaluation negatives do not become training targets without a later versioned policy decision. | OPEN_EVIDENCE_REQUIRED | G6-G10 |
| R4-R017 | Risk | Address-equality deduplication erased 65 compile-valid, content-distinct positive benchmark contracts; legacy compiler flags erased one more. | Critical | Repaired preprocessing removes address-as-duplicate deletion, preserves source provenance, uses capability-aware solc invocation, and completed a full physical rebuild. Repaired-v2 adds 47 contract identities and 71 strong cells over the audited historical baseline without synthetic negatives. | MITIGATED | G8 |
| R4-R018 | Risk | Historical single-declaration GNN extraction could select a library/non-target declaration; 341 v1 graphs mismatched preprocessing contract declarations. | High | Repaired file-level targeting is deterministic, provenance-aware and binding-checked; 22,540/22,540 repaired representations passed physical validation with requested/actual targets bound. Multi-component file semantics remain a separate open modeling risk under R4-R024. | MITIGATED | G8 |
| R4-R019 | Risk | Four-window token subsampling omits material target code. | Critical | Repaired-v2 measures 19,451/22,540 contracts over four windows. Repository-side `target_aware_guarded_v1` now guarantees no target-coverage regression relative to the historical control by falling back on ties/worse candidates; CPU comparison and identical-initialization CUDA comparison launchers are implemented without changing accepted token artifacts. Local full-population comparison evidence is pending and the selector is not promoted. | OPEN_EVIDENCE_REQUIRED | G8-G9 |
| R4-R020 | Risk | Historical final normalized-code identities were not fully grouped: 120 duplicate groups / 288 records, ten with multiple frozen group IDs. | High | Repaired-v2 rebuilt deterministic grouping and refroze roles over 11,551 leakage groups; normalized-code identity is global and role assignment is group-atomic. Grouping breadth/over-connection is separately tracked by R4-R022. | MITIGATED | G6-G8 |
| R4-R021 | Risk | Repaired source acquisition is not fully portable even if local raw manifests are byte-valid. | Medium | Versioned acquisition descriptor records exact SolidiFI source/pin, partial SmartBugs provenance, and unresolved DIVE archive/hash requirements; raw-manifest verifier remains byte authority until portable reacquisition is completed. | OPEN | G8 |
| R4-R022 | Risk | Source-scoped shared-address grouping may over-connect unrelated contracts through ubiquitous address literals or transitive address chains, reducing effective split independence and creating very large group-role footprints even though no records are deleted. | High | Read-only `r4_grouping_audit.py` and `p8_audit_grouping_breadth.py` now profile address frequency, group-size distributions, largest groups by edge reason, address-only groups, and transitive multi-address families. Local repaired-v2 grouping audit output is pending. If evidence shows over-connection, version the grouping/partition policy rather than mutating accepted v2. | OPEN_EVIDENCE_REQUIRED | G8-G9 |
| R4-R023 | Risk | Twenty-eight accepted representations use compatibility modes rather than normal full Slither analysis; seven are optimizer-active weak examples. | Medium | Compatibility/source-transform provenance remains binding-visible. `r4_sensitivity.py` and the local sensitivity profiler now build exact optimizer/model-selection compatibility sets and the bounded CUDA tooling can compare active behavior. No compatibility example is reclassified or excluded automatically; local sensitivity evidence is pending. | OPEN_EVIDENCE_REQUIRED | G8 |
| R4-R024 | Risk | File-level labels are represented by disconnected unions of all relevant inheritance leaves when one target contract cannot be proven; this preserves label ownership but changes graph semantics and can dilute/duplicate component context. | High | Repaired-v2 records component counts; 4,211 samples are multi-component, max 28, and graph maxima are 16,065 nodes / 166,459 edges. The new representation-sensitivity profiler emits exact optimizer/model-selection file-union sets and worst-case active GPU candidates; bounded forward-probe support is implemented. Local evidence is pending; any representation/architecture change requires a separate versioned decision. | OPEN_EVIDENCE_REQUIRED | G8-G9 |
| R4-B002 | Blocker | The full protected v3 contract-ID / historical-target population required to materialize the Phase-3 ledger was unavailable through GitHub during remote-only work. | High | Resolved locally: 22,493 contracts expanded to 224,930 unique rows; strict and artifact-binding validation passed; package published. | CLOSED | G3 |
| R4-B003 | Blocker | Repository-only repair could not prove repaired-v2 physical population, attrition, graph alignment, token coverage, or representation binding without protected local data/runtime artifacts. | Critical | Resolved by the accepted local repaired-v2 rebuild: 22,540 contracts, 67,620 representation files, zero missing/invalid, all 20 lineage checks PASS, binding digest `16dd4a3f...`. | CLOSED | G8 |
| R4-B004 | Blocker | After physical repaired-v2 binding, long-contract evidence and a bounded repaired-data GPU smoke had to be reviewed before any full-run decision. | Critical | Resolved as a prerequisite: the 11,341-record bounded-window experiment and repaired CUDA smoke completed. Their result was **no launch**, not full-run authorization. Remaining blockers are R4-B005/R4-B006. | CLOSED | G8 |
| R4-B005 | Blocker | The current repaired-v2 supervised objective has 899 effective loss cells, all positive target `1`, zero confirmed negatives, positive-only model selection, and no threshold/calibration/untouched-acceptance population. A 100-epoch run cannot establish discrimination quality. | Critical | Confirmed-negative evaluation research is now implemented repository-side: deterministic class-balanced pilot queue from `TRAIN_UNLABELED`, leakage-group reservation, class-specific dual-review evidence contract, and fail-closed validator. The default 25/class queue is only a yield/cost pilot; a simple zero-FP 5%-at-95% planning bound is 59 actually confirmed negatives/class. No actual negative evidence exists yet, no target `0` is authorized for training, and a PU objective has not been approved. | OPEN_EVIDENCE_REQUIRED | G8-G10 |
| R4-B006 | Blocker | Historical four-window selection is physically valid but not adequate for many repaired-v2 targets; the target-aware candidate is promising but unpromoted and has regressions. | High | A versioned research-only guarded selector, CPU population comparison, explicit target-contract span mapping, representation-sensitivity profiler, exact-control tensor identity check, identical-initialization bounded CUDA comparison, and worst-case active forward probes are implemented. Local outputs must be generated/reviewed before accepting/rejecting a new selector lineage or rebinding the full-run horizon. | OPEN_EVIDENCE_REQUIRED | G8-G9 |
