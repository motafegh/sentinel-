# R4 Risk and Blocker Register

| ID | Type | Description | Impact | Mitigation | Status | Gate |
|---|---|---|---|---|---|---|
| R4-R001 | Risk | Agent repeats prior contract audits instead of reusing them. | High | Non-duplication policy and mandatory gap IDs. | MITIGATED | G1-G4 |
| R4-R002 | Risk | Historical zeros are treated as confirmed negatives. | Critical | Phase 2 reconstructed zero origins; Phase 3 materialized all 173,384 historical-zero rows as `UNKNOWN`; Phase 5 target-zero schema requires `CONFIRMED_NEGATIVE`; Phase 6 support table contains zero confirmed-negative rows. | OPEN | G2-G9 |
| R4-R003 | Risk | Tool-correlated evidence is counted as independent confirmation. | High | Independence groups explicit; Phase 4 blind semantic review and Phase 5 aggregation policy prohibit implicit vote counting. | MITIGATED | G3-G5 |
| R4-R004 | Risk | Architecture changes distract from label repair. | High | Architecture freeze. | MITIGATED | G8 |
| R4-R005 | Risk | Threshold/calibration reuse inflates evaluation. | Critical | Phase 6 freezes `THRESHOLD_FIT` and `CALIBRATION_FIT` as `UNSUPPORTED_EMPTY`; no exposed/unknown rows may be borrowed. | OPEN | G6-G9 |
| R4-R006 | Risk | Web3Bugs declared enabled in historical config but entirely absent. | High | Phase 5 explicitly excludes Web3Bugs from the first vNext baseline. | MITIGATED | G5 |
| R4-R007 | Risk | GasException has zero trustworthy active support. | Medium | Phase 5 explicitly sets GasException to `SUPERVISION_DISABLED_PENDING_EVIDENCE`; Phase 6 assigns no supervised rows for it. | MITIGATED | G5-G8 |
| R4-R008 | Risk | 836 contracts have labels but no representations; current retry shows much of the loss is pipeline-created rather than source-intrinsic. | Critical | Phase 6 exclusion prevents loader failure, but at least 790 DIVE and seven SolidiFI normalized outputs were corrupted after the preprocessing compile gate, while two excluded SmartBugs graphs now succeed. Repair normalization, compile promoted output, rebuild representations, and re-freeze roles. | OPEN | G6-G8 |
| R4-R009 | Risk | locked_files.sha256 is stale. | Low | Stale lock is not a protected R4 artifact; current content hashes are the baseline. | OPEN | — |
| R4-R010 | Risk | Exact byte identity of the export artifact at Run12 launch is not proven from tracked evidence. | Medium | Preserve Phase-2 population reconciliation; protected historical labels hash and Phase-3 ledger are fixed lineage roots. | MITIGATED | G6-G8 |
| R4-R011 | Risk | BCCC v1.4 verified labels exist but are DEFERRED and materially disagree with raw BCCC. | High | Phase 5/6 keep BCCC deferred/not imported into the first baseline. | MITIGATED | G5-G6 |
| R4-R012 | Risk | DIVE EB TP count discrepancy in recovered historical review. | Low | Exact discrepancy preserved; Phase 5 masks DIVE EB regardless, so no current role depends on the disputed count. | MITIGATED | G4-G5 |
| R4-R013 | Risk | Historical quickstart benchmark contains invalid `NonVulnerable` mappings (including SmartBugs access_control and SolidiFI tx.origin), so SHA-clean OOD status does not imply trustworthy negative labels. | High | Phase 6 excludes quickstart from threshold/calibration/untouched-acceptance authority; use only as exposed/provenance/case-study material if needed. | MITIGATED | G6-G10 |
| R4-R014 | Risk | Historical nominal DATA label orchestration is incomplete: `sentinel-data label` remains a legacy no-op seam. | Medium | Phase 7 added a separate deterministic, versioned `sentinel_data.vnext` build/validate/load/publication path; historical `_run_label()` remains compatibility material and is not used to create vNext truth. | MITIGATED | G7 |
| R4-R015 | Risk | No trustworthy, semantically valid, unexposed untouched-acceptance corpus exists in recovered repository evidence. | Critical | Phase 6 freezes an empty `UNSUPPORTED_EMPTY_FROZEN` acceptance manifest and prohibits relabeling exposed historical/manual/quickstart/tool-silent corpora as untouched. A later promotion claim requires a new separately protected acceptance corpus. | OPEN | G6-G10 |
| R4-R016 | Risk | No trustworthy class-specific confirmed-negative support exists in policy v1, limiting model selection, threshold fitting, calibration, and final discrimination claims. | Critical | Phase 6 makes model selection positive-only limited and freezes threshold/calibration empty. Phase 8/9 must not reconstruct pseudo-negatives; later negative-evidence work requires a new versioned plan. | OPEN | G6-G10 |
| R4-R017 | Risk | Address-equality deduplication erased 65 compile-valid, content-distinct positive benchmark contracts; legacy compiler flags erased one more. | Critical | Remove address-as-duplicate behavior, make compiler flags version-aware, preserve variants in leakage families, and publish a new DATA version. | OPEN | G8 |
| R4-R018 | Risk | Single-declaration GNN extraction can select a library/non-target declaration; 341 current graphs mismatch preprocessing contract declarations. | High | Exclude libraries from target selection or represent all relevant declarations with explicit label ownership; rebuild and validate graph-label alignment. | OPEN | G8 |
| R4-R019 | Risk | Four-window token subsampling omits code in 18,491 represented contracts, including 612 / 852 optimizer cells. | Critical | Measure injection/label-site inclusion and adopt a versioned contract-aware or hierarchical long-input representation before full retraining. | OPEN | G8-G9 |
| R4-R020 | Risk | Final normalized-code identities are not fully grouped: 120 duplicate groups / 288 records, ten with multiple frozen group IDs. | High | Compute exact normalized and source-family grouping across all sources before the next role freeze. | OPEN | G6-G8 |
| R4-B002 | Blocker | The full protected v3 contract-ID / historical-target population required to materialize the Phase-3 ledger was unavailable through GitHub during remote-only work. | High | Resolved locally: 22,493 contracts expanded to 224,930 unique rows; strict and artifact-binding validation passed; package published. | CLOSED | G3 |
