# agent_proposal

**Home of `agents/` module extended-capability proposals.**

See `2026-06-04-agent-extended-capability-proposal.md` for the current proposal.

The `agents/` module is a separate Python module from `sentinel-ml` and `sentinel-data`. It provides the LangGraph orchestrator + RAG + Slither + Aderyn + graph-inspector MCP servers. It is NOT part of the v2 data module build; this directory is a sibling proposal area.
