# Stage 1: Ingestion + Preprocessing

**Status:** BLOCKED on Stage 0 confirmation.

This learning doc will be created when we start this stage. For now, see the corresponding actionable plan at `../02_stage_1_ingest_preprocess.md`.

The skeleton is complete and tested (see `stage_0_skeleton.md` for the Stage 0 deep dive). We move to this stage only after you confirm Stage 0 is mastered.
