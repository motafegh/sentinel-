# 2026-05-09 — M1 (ML Core) Plan

Spec refs: `docs/Project-Spec/SENTINEL-M1-ML.md`,
`docs/Project-Spec/SENTINEL-CONSTRAINTS.md`,
`docs/Project-Spec/SENTINEL-EVAL-BACKLOG.md`.
Status ref: `docs/STATUS.md`.

---

## 1. Current State (verified from source)

```
ml/src/
  models/
    gnn_encoder.py          # GAT + edge_emb (use_edge_attr, P0-B active)
    transformer_encoder.py  # CodeBERT + LoRA (r/alpha/dropout/target_modules cfg)
    fusion_layer.py         # FusionLayer(64) — superseded by CrossAttention
    sentinel_model.py       # CrossAttentionFusion (output_dim=128, LOCKED)
  training/
    trainer.py              # Resume/AMP/TF32/early-stop, all #11–#13/#23–#25 fixes
    focalloss.py            # FP32 cast applied (Audit #13)
  datasets/dual_path_dataset.py   # squeeze(-1) guard for legacy [E,1] graphs
  preprocessing/
    graph_schema.py         # NUM_EDGE_TYPES (single source of truth)
    graph_extractor.py      # multi_contract_policy: "first" | "by_name" (no "all")
  inference/
    api.py                  # FastAPI :8001, Prometheus, /thresholds key
    predictor.py            # windowed inference, fusion_dim from cfg, Fix #2/#4/#6/#7
    preprocess.py           # tokeniser + graph build, RAM cache (Audit #11 fixed)
    cache.py                # InferenceCache (TTL via mtime)
    drift_detector.py       # KS test; warm-up phase

ml/scripts/
  train.py                  # Full-resume CLI + --resume-reset-optimizer
  tune_threshold.py         # Per-class thresholds, fusion_dim from ckpt cfg
  validate_graph_dataset.py # Exits 0 on current 68,523 graphs
  build_multilabel_index.py
  create_splits.py / create_label_index.py / analyse_truncation.py
  run_overnight_experiments.py # Sequential MLflow sweeps
  promote_model.py          # MLflow registry promotion (M3)
  compute_drift_baseline.py # M3

ml/data_extraction/                 # Offline-only dataset-build pipeline
  ast_extractor.py        Slither-based AST → PyG graph builder (V4.3 thin wrapper)
                          Spawns 11 worker procs, writes <md5>.pt to ml/data/graphs/
                          Imports graph construction from preprocessing/graph_extractor.py
                          (single source of truth shared with inference; refactor closed
                          the silent feature-divergence bug)
  tokenizer.py            CodeBERT tokenizer + .pt serializer (V1)
                          Stores feature_schema_version metadata in output (P0-D)
                          MD5 naming aligned with ast_extractor.py output
  __init__.py             Documents files were run ONCE to build dataset; kept for
                          reproducibility, NOT part of inference pipeline

ml/tests/
  test_api / test_cache / test_dataset / test_drift_detector /
  test_fusion_layer / test_gnn_encoder / test_model /
  test_preprocessing / test_promote_model / test_trainer
```

Note: `ml/data_extraction/` and `ml/scripts/` are both M1 deliverables, but
the former is **offline batch tooling** (run once per dataset build) and the
latter is **operational tooling** (run per training run, per audit, etc.).
Plans here treat them separately.

**Active checkpoint:** `ml/checkpoints/multilabel-v3-fresh-60ep_best.pt`
**Active thresholds:** `ml/checkpoints/multilabel-v3-fresh-60ep_best_thresholds.json`
**Tuned F1-macro:** 0.5069 — v4 gate.

Open audit findings (from `SENTINEL-EVAL-BACKLOG.md`):
- #9 Med — `process_source()` temp file not cleaned on SIGKILL (`ml/src/inference/preprocess.py`)
- #10 Med — file-path vs content hashing namespaces (`ml/src/utils/hash_utils.py`)
- #12 Med — `hash_utils.py` partially dead code
- #14 Low — silent shape logging; add `SENTINEL_TRACE=1` flag
- #15 Low — `focalloss.py` docstring binary/multi-label mismatch
- #16 Low — `CLASS_NAMES` imported from `trainer.py` into `predictor.py`
- #17 Info — `peft` import-time check makes pytest verbose
- #18 Info — checkpoint `weights_only=False` still present

---

## 2. Plan A — v4 Retrain (Primary)

Goal: tuned val F1-macro **> 0.5069** on `ml/data/splits/val_indices.npy`,
no per-class drop > 0.05 F1 from v3 (per-class floors in
`SENTINEL-EVAL-BACKLOG.md` §"v3 Per-Class Floor Values").

### 2.1 Pre-flight checklist (must all pass)

- [ ] `poetry run python ml/scripts/validate_graph_dataset.py` exits 0
- [ ] `ml/data/splits/val_indices.npy` is the **same file** used by v3 (do NOT regenerate)
- [ ] MLflow experiment `sentinel-retrain-v4` exists and is empty
- [ ] No uncommitted changes to: `trainer.py`, `sentinel_model.py`,
      `gnn_encoder.py`, `transformer_encoder.py`, `dual_path_dataset.py`,
      `graph_extractor.py` (preserves comparability with v3)
- [ ] GPU free: `nvidia-smi` shows zero existing python processes
- [ ] `TRANSFORMERS_OFFLINE=1` set at shell

### 2.2 Hyperparameters (recommended starting point)

Recorded in `docs/changes/2026-05-05-v3-training-complete.md`. Repeated
here for traceability:

```bash
poetry run python ml/scripts/train.py \
    --run-name multilabel-v4-focal-r16 \
    --experiment-name sentinel-retrain-v4 \
    --epochs 60 \
    --batch-size 16 \
    --lr 3e-4 \
    --loss-fn focal --focal-gamma 2.0 --focal-alpha 0.25 \
    --lora-r 16 --lora-alpha 32 \
    --early-stop-patience 8 \
    --num-workers 2
```

Class-imbalance handling for DenialOfService (137 train support vs
IntegerUO 5,343) is the second knob to tune; see §2.4.

### 2.3 Per-class floor watch

After training, run `tune_threshold.py` and compare each class to the v3
floor. Rollback rule: if any class breaches its floor, abandon the
checkpoint regardless of macro F1. Floors:

| Class | v4 Floor (F1) |
|---|---|
| CallToUnknown | 0.3436 |
| DenialOfService | 0.3500 |
| ExternalBug | 0.3845 |
| GasException | 0.5001 |
| IntegerUO | 0.7714 |
| MishandledException | 0.4416 |
| Reentrancy | 0.4862 |
| Timestamp | 0.4289 |
| TransactionOrderDependence | 0.4270 |
| UnusedReturn | 0.4360 |

### 2.4 If v4 misses gate

In priority order:
1. Add weighted sampler for DoS/CallToUnknown (highest underrepresentation).
   Implementation: extend `dual_path_dataset.py` with a sampler builder
   keyed off `multilabel_index.csv` row sums.
2. Increase LoRA `r` to 32 (memory permitting; current 8 GB VRAM tight).
3. Try `--loss-fn bce_pos_weight` with computed per-class `pos_weight`
   tensor (already supported by `trainer.py`).
4. Only then: revisit data — re-extract graphs with a different
   `multi_contract_policy` (out of scope unless §6 is approved first).

---

## 3. Plan B — Autoresearch Harness (parallel, unblocks v4+)

Goal: turn v4 from a hand-launched run into a programmatic sweep.

### 3.1 New files

```
ml/scripts/auto_experiment.py        NEW — thin CLI that:
  • parses --focal-gamma --lora-r --weighted-sampler etc.
  • invokes the same TrainConfig path as train.py
  • after training, runs tune_threshold.py and reads tuned F1
  • prints SENTINEL_SCORE=<float> as the LAST stdout line (autoresearch contract)
  • exits non-zero if pre-flight checks fail

ml/autoresearch/program.md           NEW — declarative search spec:
  metric:          tuned F1-macro (sentinel-retrain-v4)
  constraints:     no per-class F1 below v3 floor
  knobs:           focal_gamma, lora_r, weighted_sampler, lr, batch_size
  ranges:          gamma ∈ [1.0, 3.0], r ∈ {8,16,32}, lr ∈ [1e-4, 5e-4]
  budget:          12 runs total, sequential
  rollback:        any run with score < 0.5069 is discarded
```

### 3.2 Validation

A 1-epoch dry-run must:
- emit `SENTINEL_SCORE=<float>` on the last stdout line
- create exactly one MLflow run under `sentinel-retrain-v4`
- not modify any file in `ml/checkpoints/` if `--dry-run` is passed

---

## 4. Plan C — Open audit hardening (background)

Pick up after v4 lands; none of these block training.

| # | Action | File |
|---|---|---|
| 9 | Wrap `process_source()` body in `try/finally` that unlinks the temp file; add SIGTERM handler that cleans `Path("/tmp/sentinel_*").unlink()` matching pattern | `ml/src/inference/preprocess.py` |
| 10/12 | Decide single hashing strategy (recommend: content-hash always); delete unused functions in `hash_utils.py` | `ml/src/utils/hash_utils.py` |
| 14 | Read `os.environ.get("SENTINEL_TRACE")=="1"` once at module load; gate shape `logger.debug` calls behind the flag | shared util |
| 15 | Rewrite `FocalLoss` docstring: clarify it operates on per-class logits in multi-label setting | `ml/src/training/focalloss.py` |
| 16 | Move `CLASS_NAMES` to `ml/src/preprocessing/labels.py`; import from there in both `trainer.py` and `predictor.py` | new file + 2 edits |
| 17 | Move `peft` import inside the function that needs it; pytest stays quiet | `ml/src/models/transformer_encoder.py` |
| 18 | Investigate moving to `safetensors` for non-LoRA portions; document why LoRA still needs pickle | trainer + predictor |

### 4.bis Data-extraction tooling (`ml/data_extraction/`)

The dataset built by `ast_extractor.py` + `tokenizer.py` will be regenerated
only if a feature-schema change forces it (P0-D bumps `feature_schema_version`).
Two improvements worth tracking but not blocking on:

- **Reproducibility manifest** — at end of `ast_extractor.py` run, write
  `ml/data/graphs/_manifest.json` containing: solc versions used, feature
  schema version, total files written, run timestamp, source parquet hash.
  Currently this information is implicit in DVC pointers only.
- **Tokenizer schema-version assertion** — at start of `tokenizer.py`,
  read `_manifest.json` from graphs dir and assert it matches the tokenizer's
  expected `feature_schema_version`. Prevents tokens-from-old-schema being
  paired with new graphs (silent training accuracy regression).

These belong to a *future* re-extraction (next time the schema bumps), not
to the v4 retrain — v4 reuses v3's data unchanged.

---

## 5. Plan D — Single-contract limitation (post-M6, see ROADMAP Move 9)

Tracked here only for completeness; do not start before M6 ships.

`graph_extractor.GraphExtractionConfig.multi_contract_policy` already
supports `"first" | "by_name"`. Adding `"all"` requires:
- `_select_contract()` returns `list[Data]` when policy=`"all"`
- `preprocess.process_source_all_contracts()` new entry point
- `predictor.predict_source_multi()` with max-aggregation across contracts
- `api.PredictResponse.contracts_analysed: list[str]` (new optional field)
- `cache.py` keying strategy: per-contract sub-keys
- `ml/README.md` Known Limitation #2 update

---

## 6. Acceptance Criteria

- v4 checkpoint saved as `ml/checkpoints/multilabel-v4-<runname>_best.pt`
- `<...>_thresholds.json` written by `tune_threshold.py`
- MLflow run shows tuned F1 > 0.5069 and all per-class floors held
- `promote_model.py --stage Staging --val-f1-macro <v4>` succeeds
- `docs/STATUS.md` "Active Checkpoint" block updated; new dated changelog
  written summarising the run
